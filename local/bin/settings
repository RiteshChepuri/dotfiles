#!/bin/sh

CHOSEN=$(printf "Wifi\nBluetooth" | tofi --config "$HOME"/.config/tofi/powermenu-config)

case "$CHOSEN" in
    "Wifi") rofi-wifi-menu.sh ;;
    "Bluetooth") blueman-manager ;;
    *) exit 1 ;;
esac
